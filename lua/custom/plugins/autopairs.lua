-- autopairs
-- https://github.com/windwp/nvim-autopairs

return {
  'windwp/nvim-autopairs',
  event = 'InsertEnter',
  -- blink.cmp adds `(` after accepting functions (completion.accept.auto_brackets)
  opts = {},
}
